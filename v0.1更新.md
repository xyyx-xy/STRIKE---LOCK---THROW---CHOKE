# v0.1 更新日志(2026-09-19)

## 正蹬两阶段化(右键)

- 踢击从单段正弦摆腿改为真实两阶段发力:`rig.js` pose 踢击分支重写。
  - 0–0.16s 提膝回收蓄力(脚回收、膝盖抬高)。
  - 0.16–0.34s 小腿弹出蹬出(ease-out 爆发,有效帧 .25 落在弹射中段)。
  - 0.34–0.42s 定帧保持,0.42–0.65s 收势落回。
- 发力时躯干与手臂联动:胸后仰、送胯、双臂后摆、双拳握紧(.95)保持平衡。

## 第一人称踢腿可见

- `game.js` 新增画面坐标关键帧踢腿(与第一人称手部同一 viewmodel 技术):提膝抬到画面中下方,蹬出时整条小腿冲向画面中心,再收落。
- 静止/移动时腿部依旧不入画,不挡视野(可见性由 `viewmodel.js` 门控)。

## 掌拍手型修正

- 掌击出手瞬间手型竖直化:手指朝上、手背朝自己、掌根推向对手;待机手型不变。
- 修正前手掌横躺、拇指朝上,掌击像耳光。

## 测试与缓存

- `combat.test.mjs` 新增用例:踢击有效帧(.25s)的脚必须投影在第一人称相机画面内,防止后续迭代把踢腿调出画面。当前 10/10 通过。
- 模块引用加版本查询参数防缓存:`index.html` 与 `game.js` 现为 `?v=043`,更新源码后递增即可。
