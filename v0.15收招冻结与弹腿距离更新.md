# v0.15 弹腿距离对齐正蹬

- `snap-kick.js:3` `SNAP_KICK.range` 2.15 → 2.65，与正蹬 `ATTACKS.kick` 相同射程；其余参数不变。
- 修复 NPC 攻击命中后不再行动的冻结：`combat.js:66` `advanceEnemyAttack` 收招计时检查提升到函数顶层，与单次命中判定同级；并行改动后仍完好。
- 清理调试期临时导出 `window.__blood/__scene/__p/__s`，现无残留。
- 缓存版本 `game.js?v=060`；全部规则测试通过。
