# v0.9.6 收招冻结修复与调试清理

- 修复 NPC 攻击命中后不再行动的冻结：`combat.js:48` `advanceEnemyAttack` 的收招计时检查提升到函数顶层，与单次命中判定同级，不再嵌在首次命中分支内。
- 血液方块特效修复完成，本轮未再改动 `fx.js`。
- 清理 `game.js` 临时调试导出 `window.__blood`、`__scene`、`__p`、`__s`。
- 缓存版本 `game.js?v=059`。
